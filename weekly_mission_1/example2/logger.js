// creamos una función para guardar el modulo info
exports.info = (message) => {
    console.log(`info: ${message}`)
}

// creamos una nueva función para guardar el modulo verbose
exports.verbose = (message) => {
    console.log(`verbose: ${message}`)
}